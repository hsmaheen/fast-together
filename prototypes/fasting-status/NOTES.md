# Fasting Status Prototype

Question: What should the first local-only Fasting Status screen feel like before we build it in Flutter?

This is disposable HTML/CSS prototype code. It is not production Flutter code and should be deleted or absorbed after a direction is chosen.

Run:

```sh
open prototypes/fasting-status/index.html
```

Variants:

- `A` - Quiet Timer
- `B` - Partner Check-In
- `C` - Time Ledger

Palettes:

- `amber` - Warm Amber from Material Theme Builder export (`#B58C52` seed)
- `natural` - Natural Olive from Material Kolor export (`#7B7660` seed)
- `cobalt` - Earlier cool private cobalt exploration

Session states:

- `ready` - Before the user starts a Fasting Session
- `fasting` - Active Fasting Session

Decision capture:

- Winner: TBD
- What to keep: TBD
- What to reject: TBD
