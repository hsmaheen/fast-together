# Defer manual past-session creation

Users should eventually be able to create a Fasting Session after the fact, but this is outside the MVP. The MVP supports starting an active session with a user-set start time and correcting the actual end time of ended sessions, which covers common timing mistakes without adding a standalone manual history-entry workflow in the first release.
