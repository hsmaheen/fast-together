# Use lightweight Domain-Driven Design

The MVP will use lightweight Domain-Driven Design: shared domain language, behavior-rich domain objects, and clear seams where they are earned. This fits the fasting domain terms already captured in `CONTEXT.md` while avoiding unnecessary DDD ceremony such as microservices, event sourcing, CQRS, large folder hierarchies, or repository abstractions before persistence makes them useful.
